# ORDER_VERIFICATION_SECRET: Secret key used by the Vercel serverless API (/api/verify-order)
# to compute cryptographic HMAC-SHA256 signatures for authenticating orders.
ORDER_VERIFICATION_SECRET="your_production_order_verification_secret_key"
